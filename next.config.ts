import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Emits a static HTML/CSS/JS export in the `out/` directory
  output: "export",
  // Sets the base path for GitHub Pages subpath deployment
  basePath: "/Janarthanan_P__portfolio",
};

export default nextConfig;
